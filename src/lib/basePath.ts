/**
 * Debe coincidir con `basePath` en `next.config.ts`.
 * Se usa en `<img src>` (el optimizador de `next/image` no resuelve bien `src` + basePath en algunos entornos).
 */
export const APP_BASE_PATH = "/desarrollo-social";
