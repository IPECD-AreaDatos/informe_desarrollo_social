# Copiá a .env.local para `npm run dev` en tu Mac (Next fuera de Docker).
# Next.js carga .env.local después de .env y sobrescribe estas claves.

DB_HOST=127.0.0.1
DB_PORT=3307
PG_HOST=127.0.0.1
PG_PORT=5433
